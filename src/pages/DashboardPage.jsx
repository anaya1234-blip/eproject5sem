/**
 * DashboardPage.jsx
 * Enhanced version of Dashboard that uses the DashboardStats
 * and RecentActivity sub-components for a cleaner separation.
 * Kept as an alias/redirect to Dashboard for backward compatibility.
 */
export { default } from './Dashboard';
